// Styles exports - експорт всіх стилів
export * from './constants';

// Аліаси для зручності імпорту
export {
    COLORS,
    FONTS,
    FONT_SIZES,
    SPACING,
    BORDER_RADIUS,
    BUTTON_SIZES,
    SHADOWS,
    BREAKPOINTS,
} from './constants';

// Приклад компонента (тільки для розробки)
export { default as ConstantsExample } from './constants.example';
